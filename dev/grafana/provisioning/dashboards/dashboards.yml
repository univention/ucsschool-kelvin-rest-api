# Dev-only Grafana dashboard provider: load JSON dashboards from disk.
apiVersion: 1

providers:
  - name: kelvin
    orgId: 1
    folder: ""
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /var/lib/grafana/dashboards
