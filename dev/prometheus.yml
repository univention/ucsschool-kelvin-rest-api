# Dev-only Prometheus config: scrape the OpenTelemetry Collector's Prometheus
# exporter, which re-exposes the metrics Kelvin sends over OTLP.
global:
  scrape_interval: 5s

scrape_configs:
  - job_name: otel-collector
    static_configs:
      - targets: ["otel-collector:8889"]
